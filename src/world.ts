
import * as BABYLON from 'babylonjs'

export function setupWorld(canvas: HTMLCanvasElement) {  
    
  const engine = new BABYLON.Engine(canvas, true);

  console.log(engine.version);
  
  // code continues....}
}