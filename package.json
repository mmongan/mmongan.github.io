{
  "name": "babylon-webxr-ar-menu",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git add -A && git commit -m \"build: update compiled assets\" || true && git push origin main"
  },
  "homepage": "https://mmongan.github.io",
  "dependencies": {
    "@babylonjs/core": "^6.0.0",
    "@babylonjs/gui": "^6.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "typescript": "^5.0.0",
    "gh-pages": "^5.0.0"
  }
}
