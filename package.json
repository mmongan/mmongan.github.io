{
  "name": "babylon-webxr-ar-menu",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "copy-dist": "node scripts/copy-dist.js",
    "build:publish-local": "npm run build && npm run copy-dist",
    "deploy": "npm run build && npm run copy-dist && git add -A && git commit -m \"build: update compiled assets\" || true && git push origin main"
  },
  "homepage": "https://mmongan.github.io",
  "dependencies": {
    "@babylonjs/core": "^6.0.0",
    "@babylonjs/gui": "^6.0.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "playwright": "^1.58.2",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
